- name: Create a jenkins job
  community.general.jenkins_job:
    config: "{{ lookup('template', 'templates/pipeline.xml.j2') }}"
    name: test
    url: http://localhost:8080
    user: admin
    password: admin123
  loop :
  - { name : 'frontend', token : 'frontend1', url : 'frontend1' }